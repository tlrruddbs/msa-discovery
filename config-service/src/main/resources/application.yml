spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        bootstrap: true
        native:
          search-locations: file:///C:/Users/tlrru/IdeaProjects/msa-discovery
        git: #default
          uri: https://github.com/tlrruddbs/msa-discovery
          default-label: main
server:
  port: 8888